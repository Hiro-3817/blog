baseURL: "https://hiro-3817.github.io/blog/"
languageCode: "ja"
title: "HNEST"

pagination:
  pagerSize: 10

# 基本設定
enableRobotsTXT: true
canonifyURLs: true
buildDrafts: false
buildFuture: false
buildExpired: false

# 出力設定（RSS/Sitemap/Robots）
outputs:
  home: [HTML, RSS]

# パーマリンク（PaperMod推奨のシンプル構成）
permalinks:
  posts: "/posts/:slug/"

theme:
  - PaperMod

# PaperMod params（見た目・SEO・SNS）
params:
  description: "備忘録"
  keywords: ["Hugo", "PaperMod", "GitHub Pages", "ブログ"]
  env: "production"
  defaultTheme: "auto"
  disableSpecial1stPost: true
  ShowReadingTime: true
  ShowShareButtons: true
  ShowCodeCopyButtons: true
  ShowToc: true
  TocOpen: false
  images: ["images/og-image.png"]
  author: "HNEST"
  comments: false

  # ホーム情報カード
  homeInfoParams:
    Title: "こんにちは 👋"
    Content: "Hugo + PaperModでブログを運用しています。"

  # ソーシャルリンク（assets/icons/*.svg でカスタムアイコンを使用可能）
  socialIcons:
    - name: github
      url: "https://github.com/Hiro-3817"
    - name: x
      url: "https://x.com/username"
    - name: rss
      url: "/index.xml"

  # カスタムCSS（カード背景を調整したい場合など）
  customCSS:
    - css/extended/custom.css

# メニュー
menu:
  main:
    - name: Home
      url: /blog/
      weight: 1
    - name: Posts
      url: /blog/posts/
      weight: 2
    - name: Tags
      url: /blog/tags/
      weight: 3
    - name: About
      url: /blog/about/
      weight: 4

# タクソノミー
taxonomies:
  tag: tags
  category: categories

# Sitemap
sitemap:
  changefreq: daily
  priority: 0.5
  filename: sitemap.xml

# Minify
minify:
  disableXML: true
  minifyOutput: true

# マークアップ（コードハイライト＆生HTML）
markup:
  highlight:
    noClasses: false
  goldmark:
    renderer:
      unsafe: true

# プライバシー設定（外部サービスの追跡を抑制）
privacy:
  x:
    enableDNT: true
  youtube:
    privacyEnhanced: true

# GitHub PagesでのベースURL（プロジェクトサイト例）
# baseURL: "https://username.github.io/repository-name/"  # プロジェクトサイトの場合はこちらに切り替え
